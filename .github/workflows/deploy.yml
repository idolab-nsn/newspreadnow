name: Deploy
on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
concurrency:
  group: deploy
  cancel-in-progress: true
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Placeholder
        run: echo "[OK] Deploy Placeholder Running"
