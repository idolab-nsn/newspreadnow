name: Content
on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
jobs:
  content:
    runs-on: ubuntu-latest
    steps:
      - name: Run Placeholder
        run: echo "[OK] Content Workflow Running"
